This program sorts the array in acending order.
The program consists of following steps:
Accept the size of the array from the user
Ask the user to enter the array elements
Sorts the array
Display the sorted array
